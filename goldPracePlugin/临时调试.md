# 🔍 调试竖线问题

## 请老板帮我确认

### 步骤 1: 打开扩展popup

### 步骤 2: 按 F12 打开 popup 的开发者工具

### 步骤 3: 查看 Console 输出

应该看到类似：
```
📊 交易中 - 当前时间:13:42, fullDayData:XXX点, dataToRender:YYY点
   最后一个绘制点: 13:42
```

**请告诉我**：
1. fullDayData 有多少点？
2. dataToRender 有多少点？
3. 最后一个绘制点的时间是多少？

---

### 步骤 4: 查看具体数据

在 popup 的 Console 执行：
```javascript
// 查看数据详情
chrome.runtime.sendMessage({ action: 'getData' }, response => {
  const data = response.fullDayData;
  console.log('fullDayData.times 前10个:', data.times.slice(0, 10));
  console.log('fullDayData.times 后10个:', data.times.slice(-10));
  console.log('fullDayData.data 前10个:', data.data.slice(0, 10));
  console.log('fullDayData.data 后10个:', data.data.slice(-10));
});
```

把结果告诉我！

